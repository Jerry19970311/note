pluginManagement {
    repositories {
        mavenCentral()
        jcenter()
        maven { url 'https://plugins.gradle.org/m2' }
    }
}

rootProject.name = 'note'

include("server")
include("android")